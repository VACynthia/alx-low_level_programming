#include <stdio.h>

/**
 * main - the main function
 *
 * print_alphabet - program that prints the alphabet in lowercase
 *
 * Return: always 0
 */

int main(void)
{
	int i  = (a..z);

	putchar(i);

	return (0);
}
